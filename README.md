To run the program,
Create a Codespace
In the Terminal, first install the latest version of node by adding command: "nvm install 18"
After installing, type "nvm use 18"
To run the node, first install the node by commanding "npm install"
After installation, run the code as developer by commanding "npm run dev"
this shows the local host link, you can open and access the website.
